This repo contains all the header files from various games' SDKs.

This is purely for convenience when compiling QMM or any plugin.

Some of these have been modified to fix compilation issues (usually functions/enums commented out).

There is no attempt to claim ownership of any of these files.
